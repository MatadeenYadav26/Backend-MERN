const express = require("express")

const app = express() // server is created , note: server is created but not started yet!

app.listen(3000) // app.listen() server start karta hai , 3000 is port , well undrstand in next class

